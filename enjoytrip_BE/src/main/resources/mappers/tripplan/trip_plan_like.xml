<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.trip.domain.tripplan.repository.TripPlanLikeDao">
    <insert id="insert">
        insert into trip_plan_like(trip_plan_id, member_id, created_at)
        values(#{tripPlanId}, #{memberId}, now())
    </insert>

    <update id="delete">
        update trip_plan_like
        set
            deleted_at = now()
        where trip_plan_id=#{tripPlanId} and member_id=#{memberId}
    </update>

    <select id="exists">
        select count(*)
        from trip_plan_like
        where trip_plan_id = #{tripPlanId} and member_id = #{memberId}
    </select>

    <update id="reInsert">
        update trip_plan_like
        set
            deleted_at = null,
            created_at = now()
        where trip_plan_id = #{tripPlanId} and member_id = #{memberId}
    </update>
</mapper>