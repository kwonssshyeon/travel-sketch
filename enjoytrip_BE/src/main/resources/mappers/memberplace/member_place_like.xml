<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.trip.domain.memberplace.repository.MemberPlaceLikeDao">
    <insert id="insert">
        insert into member_place_like(member_place_id, member_id, created_at)
        values(#{memberPlaceId}, #{memberId}, now())
    </insert>

    <update id="delete">
        update member_place_like
        set
            deleted_at = now()
        where member_place_id=#{memberPlaceId} and member_id=#{memberId}
    </update>

    <select id="exists">
        SELECT count(*)
        FROM member_place_like
        WHERE member_place_id = #{memberPlaceId} AND member_id = #{memberId}
    </select>

    <update id="reInsert">
        UPDATE member_place_like
        SET
        deleted_at = null,
        created_at = now()
        WHERE member_place_id = #{memberPlaceId} AND member_id = #{memberId}
    </update>
</mapper>